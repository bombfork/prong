@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/prongTargets.cmake")

check_required_components(prong)

# Provide convenience variables
set(PRONG_INCLUDE_DIRS "${PACKAGE_PREFIX_DIR}/include")
set(PRONG_LIBRARIES bombfork::prong)

# Report configuration
if(NOT prong_FIND_QUIETLY)
    message(STATUS "Found Prong: ${PACKAGE_PREFIX_DIR} (version ${PACKAGE_VERSION})")
endif()
